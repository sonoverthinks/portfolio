// // function Typography({ children }) {
// //   return <p>{children}</p>;
// // }
// const components = {};

// const headings = ["h1", "h2", "h3", "h4", "h5", "h6"];
// const length = headings.length;

// headings.forEach((tag, index) => {
//   const pixel = 32 - 3.2 * index;

//   components[tag] = ({ children }) => {
//     <p className={`text-[${pixel}px]`}>{children}</p>;
//   };
// });

// export default components;
